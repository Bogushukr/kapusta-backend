// const express = require('express')
// const router = express.Router()

// const { cashController } = require('../../controller')
// const guard = require('../../helpers/guard')


// router
    // .get('/', guard, cashController.getAll)
    // .get('/:cashId', guard, cashController.getById)
    // .post('/', guard, createContact, cashController.add)
    // .delete('/:cashId', guard, cashController.remove)
    // .get('/:date', guard, cashController.getDate)
    // .get('/:category', guard, cashController.getCategory)

    


// module.exports = router